services:
  footy:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=data/footy.duckdb
    restart: unless-stopped
